Necessary features:
    - login support
        - TODO: timestamps on forwarded token

Things to add:
    - separate questions about cache parameters
        - maybe: lead into cache access pattern questions
    - add splitting up address apppropriately to the cache access patterns
    - ? add what is evicted to the cache access pattern question
    - final state of the cache questions?

    - selecting accesses based on % hit/miss/conflict miss target?
        should allow for huge cache problems?

Also consider:
    - question with C code?
